<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Интернет и сеть (обязательно для загрузки тайлов OSM) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Геолокация -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Хранение (для кэширования тайлов карты) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.DrawMap"
        android:usesCleartextTraffic="true"> <!-- Важно для загрузки тайлов по HTTP -->

        <!-- Splash Screen -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DrawMap">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Home Screen -->
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false" />

        <!-- Gallery Screen (добавлено) -->
        <activity
            android:name=".ui.gallery.GalleryActivity"
            android:exported="false" />

        <!-- Route detail -->
        <activity
            android:name=".ui.route.RouteDetailActivity"
            android:exported="false" />

        <!-- Photo fullscreen -->
        <activity
            android:name=".ui.photo.PhotoActivity"
            android:exported="false" />

        <!-- Route fullscreen -->
        <activity
            android:name=".ui.route.RouteFullScreenActivity"
            android:exported="false" />

    </application>
</manifest>